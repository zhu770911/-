# 智能停车云平台 — 新人上手指南

## 1. 环境要求

| 工具 | 版本 | 说明 |
|------|------|------|
| Java | 17+ | 推荐 Zulu OpenJDK |
| Maven | 3.6+ | |
| MySQL | 8.0+ | |
| Redis | 7.0+ (Windows版可) | |
| Node.js | 18+ | 管理端前端 |
| 微信开发者工具 | 最新稳定版 | 小程序调试 |

## 2. 数据库初始化

```sql
-- 1. 登录 MySQL
mysql -u root -p

-- 2. 创建数据库
CREATE DATABASE smart_parking DEFAULT CHARACTER SET utf8mb4;

-- 3. 导入建表 + 测试数据
USE smart_parking;
SOURCE 项目路径/smart-parking/backend/src/main/resources/init.sql;
```

> init.sql 会自动创建 4 张表 + 330 个测试车位 + 管理员账号

## 3. 修改配置

编辑 `smart-parking/backend/src/main/resources/application.yml`：

```yaml
spring.datasource.password: 你的MySQL密码   # ← 改这里
spring.data.redis.password: 你的Redis密码   # ← 改这里（没有密码就删掉这行）
```

## 4. 启动服务

```bash
# 终端1：启动 Redis（Windows）
redis-server.exe

# 终端2：启动后端（首次需要下载依赖，稍等）
cd smart-parking/backend
mvn spring-boot:run
# 看到 "Started SmartParkingApplication" 即成功 → http://localhost:8080

# 终端3：启动管理端
cd smart-parking/admin-frontend
npm install    # 仅首次
npm run dev    # → http://localhost:3000
```

## 5. 使用小程序

1. 打开微信开发者工具 → 导入项目
2. 目录选 `smart-parking/miniapp`
3. AppID 选「测试号」
4. 详情 → 本地设置 → 勾选「不校验合法域名」

## 6. 默认账号

| 角色 | 手机号 | 密码 |
|------|--------|------|
| 管理员 | 13800000000 | admin123 |
| 普通用户 | 13900001111 | 123456 |

## 7. 遇到问题？

- 端口冲突：改 `application.yml` 中的 `server.port`
- 编译报错：检查 Java 版本是否 ≥ 17
- 小程序 401：检查「不校验合法域名」是否已勾选
- npm 下载慢：`npm config set registry https://registry.npmmirror.com`
