# 智能停车云平台 — 项目说明

## 系统架构

```
微信小程序（车主）  →  后端 API (Spring Boot)  ←  管理后台（运营方）
       📱                    💻 8080                   🖥️ 3000
                              ↕
                         MySQL + Redis
```

## 核心功能

| 模块 | 功能 |
|------|------|
| 👤 用户 | 手机号注册/登录、JWT 认证 |
| 🅿️ 停车 | 查看附近停车场、车位状态筛选（公开访问） |
| 📋 预约 | 预约车位 → 手动支付 → 自动扣减库存（Redis锁+乐观锁防超卖） |
| 📦 订单 | 状态流转：待支付→已预约→已入场→已完成→已取消 |
| ⚙️ 管理 | 订单管理、停车场CRUD、车位管理、数据看板 |
| 📊 统计 | 今日订单/营收/利用率 + 近7日趋势图 + 车位分布饼图 |

## 运行方式

启动三个服务后，打开浏览器：

| 页面 | 地址 | 说明 |
|------|------|------|
| 管理后台 | `http://localhost:3000` | 管理员登录，管理订单和车位 |
| API 调试 | `http://localhost:8080/doc.html` | 网页版 API 测试工具（中文） |
| API 文档 | `http://localhost:8080/swagger-ui/index.html` | 标准 Swagger 文档 |

**默认账号**：
| 角色 | 手机号 | 密码 |
|------|--------|------|
| 管理员 | 13800000000 | admin123 |
| 普通用户 | 13900001111 | 123456 |

## 项目目录

```
smart-parking/
├── backend/            后端 Spring Boot（44个源文件）
│   ├── config/         配置（CORS、JWT拦截、MyBatis-Plus、Jackson）
│   ├── controller/     控制器（User、ParkingLot、Booking、Order、Admin）
│   ├── service/        业务逻辑（用户、预约、订单、管理、统计）
│   ├── entity/         实体类（User、ParkingLot、ParkingSlot、Order、Payment）
│   ├── dto/            数据传输对象
│   ├── mapper/         数据访问层
│   └── utils/          工具类（JWT）
├── admin-frontend/     管理后台 Vue 3 + Element Plus + ECharts
│   ├── views/          页面（Login、Layout、Dashboard、Parking、Orders）
│   ├── api/            API 封装
│   └── router/         路由守卫
└── miniapp/            微信小程序（5个页面）
    ├── pages/          首页、车位详情、预约、订单、我的
    └── utils/          API 封装
```

## API 清单（20个接口）

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|:----:|
| POST | `/api/user/register` | 注册 | ❌ |
| POST | `/api/user/login` | 登录 | ❌ |
| GET | `/api/user/info` | 个人信息 | ✅ |
| GET | `/api/parking/list` | 停车场列表 | ❌ |
| GET | `/api/parking/{id}` | 停车场详情 | ❌ |
| GET | `/api/parking/{id}/slots` | 车位列表 | ❌ |
| POST | `/api/booking/reserve` | 预约车位 | ✅ |
| PUT | `/api/booking/pay/{id}` | 支付订单 | ✅ |
| PUT | `/api/booking/cancel/{id}` | 取消预约 | ✅ |
| GET | `/api/order/my` | 我的订单 | ✅ |
| GET | `/api/order/{id}` | 订单详情 | ✅ |
| GET | `/api/admin/orders` | 管理端订单 | ✅ |
| PUT | `/api/admin/order/{id}/enter` | 确认入场 | ✅ |
| PUT | `/api/admin/order/{id}/leave` | 确认离场 | ✅ |
| POST | `/api/admin/parking` | 新增停车场 | ✅ |
| PUT | `/api/admin/parking/{id}` | 编辑停车场 | ✅ |
| DELETE | `/api/admin/parking/{id}` | 删除停车场 | ✅ |
| POST | `/api/admin/slot` | 新增车位 | ✅ |
| PUT | `/api/admin/slot/{id}/status` | 修改车位状态 | ✅ |
| GET | `/api/admin/statistics` | 数据统计 | ✅ |

## 技术亮点

- **防超卖**：Redis 分布式锁 + 乐观锁（CAS UPDATE）双重保障
- **订单状态机**：5 种状态完整流转，不可逆操作
- **分页查询**：手动实现 COUNT + LIMIT，兼容 MyBatis-Plus 3.5.11
- **统计看板**：ECharts 图表 + 实时数据刷新
- **多日期格式兼容**：自定义 Jackson 反序列化器

## 相关文档

- [启动指南（IDEA）](./启动.md)
- [需求规格说明书](./智能停车云平台_需求文档.md)
- [开发环境记录](./开发环境.md)
- [配置说明（上手指南）](./配置说明.md)
- [项目进度](./项目进度.md)
