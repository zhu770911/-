# 智能停车云平台 — IDEA 启动指南

> 适用环境：IntelliJ IDEA 2026.1+ · Windows 11 · JDK 17+

---

## 1. 前置准备

确保以下服务已启动：

| 服务 | 端口 | 如何启动 |
|------|------|----------|
| MySQL | 3306 | `net start MySQL80`（管理员命令行）或服务面板 |
| Redis | 6379 | 双击 `D:\Java\redis\...\redis-server.exe` |

数据库已创建（第一次按下方步骤初始化，后续无需重复）。

---

## 2. 数据库初始化（仅首次）

```sql
-- 用 Navicat 或 IDEA 内置 Database 工具连接 localhost:3306，root / 123456
-- 然后执行 init.sql（位于 backend/src/main/resources/init.sql）
```

> `init.sql` 会创建 smart_parking 库（若不存在则手动 `CREATE DATABASE`）+ 5 张表 + 330 个测试车位 + 2 个测试账号。

测试账号：

| 角色 | 手机号 | 密码 |
|------|--------|------|
| 管理员 | 13800000000 | admin123 |
| 普通用户 | 13900000001 | 123456 |

---

## 3. IDEA 打开项目

1. **File → Open** → 选择 `smart-parking/backend` 目录
2. IDEA 自动识别为 Maven 项目，等待依赖下载完成（右下角进度条消失）
3. 若提示 JDK 未配置：**File → Project Structure → SDK** → 添加 `D:\Java\JDK`（Zulu OpenJDK 25）

---

## 4. 启动后端

### 4.1 确认配置

打开 `src/main/resources/application.yml`，确认 MySQL 密码与本机一致：

```yaml
spring:
  datasource:
    password: 123456   # ← 改成你的 MySQL 密码
  data:
    redis:
      host: localhost
      port: 6379
      #password:        # ← 本机 Redis 无密码，保持注释
```

### 4.2 运行

1. 在项目树中找到 `com.smartparking.SmartParkingApplication`
2. 右键 → **Run 'SmartParkingApplication'**（或点击 main 方法旁的绿色三角）
3. 控制台出现以下输出即成功：

```
Started SmartParkingApplication in X.X seconds
智能停车云平台后端服务启动成功！
中文接口文档: http://localhost:8080/doc.html
```

### 4.3 验证

浏览器打开 [http://localhost:8080/doc.html](http://localhost:8080/doc.html)，能看到中文 API 调试页面。

---

## 5. 启动管理端前端

### 5.1 首次安装依赖

```bash
cd smart-parking/admin-frontend
npm install
```

### 5.2 启动开发服务器

- **方式一（推荐）**：IDEA 内置终端
  1. 打开 Terminal（View → Tool Windows → Terminal）
  2. `cd ../admin-frontend`（从 backend 目录切过去）
  3. `npm run dev`

- **方式二**：VS Code 打开 `admin-frontend` 目录，终端中 `npm run dev`

- **方式三**：Git Bash 中操作（路径 `/d/课程/大三下/小学期/smart-parking/admin-frontend/`）

启动后访问 [http://localhost:3000](http://localhost:3000)，用管理员账号登录。

---

## 6. 启动微信小程序

1. 打开**微信开发者工具**
2. 导入项目 → 目录选择 `smart-parking/miniapp`
3. AppID 选「**测试号**」（或使用自己的 AppID）
4. 详情 → 本地设置 → 勾选「**不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书**」
5. 点击「编译」即可预览

---

## 7. 三端启动后检查清单

| 服务 | 地址 | 验证方式 |
|------|------|----------|
| 后端 API | http://localhost:8080 | 浏览器打开，有 JSON 响应 |
| API 调试 | http://localhost:8080/doc.html | 中文 Swagger 页面 |
| 管理端 | http://localhost:3000 | 能看到登录页 |
| 小程序 | 开发者工具 | 首页显示 3 个停车场 |

---

## 8. 常见问题

### IDEA 编译报错：Lombok 不可用
> 本项目已移除 Lombok，全部手写 getter/setter，无需安装 Lombok 插件。

### IDEA 运行报 `--add-opens` 相关错误
> 项目 `.mvn/jvm.config` 已配置 JDK 25 所需的 JVM 参数，无需手动处理。  
> 如果用 JDK 21 或 17，可以删除该文件或注释掉内容。

### 端口被占用
```bash
# Windows 查看端口占用
netstat -ano | findstr "8080"
# 记下 PID，然后
taskkill /F /PID <PID>
```

### Maven 下载慢
> `~/.m2/settings.xml` 已配置阿里云镜像，无需重复操作。

### npm 下载慢
```bash
npm config set registry https://registry.npmmirror.com
```

### 小程序请求失败
> 检查微信开发者工具 → 详情 → 本地设置 → **「不校验合法域名」是否已勾选**。

### Redis 连接失败
> 确认 `redis-server.exe` 已启动（看到一个 ASCII 图标输出即表示运行中）。

### 如何关闭开发服务器？

**正常关闭**：
- 后端（Spring Boot）：IDEA 控制台点红色方块停止
- 管理端（npm）：运行 `npm run dev` 的终端中按 `Ctrl + C`
- Redis：直接关闭 `redis-server.exe` 窗口

**终端已关闭的补救**（进程仍在后台占用端口）：
```bash
# 找到占用端口的进程 PID
netstat -ano | findstr "3000"    # 管理端前端
netstat -ano | findstr "8080"    # 后端

# 强制结束（把 <PID> 换成上一步最后一列的数字）
taskkill /F /PID <PID>
```

### 小程序拒绝了定位权限，如何重新开启？

**开发者工具（模拟器）**：
- 点击模拟器右上角「清缓存」→ 勾选「清除授权数据」→ 重新编译，再次弹出定位授权弹窗
- 或：模拟器顶部「···」→ 设置 → 找到定位权限 → 切换为"允许"

**真机（手机微信）**：
- 小程序右上角「···」→ 设置 → 打开「位置信息」
- 或：手机系统设置 → 微信 → 位置 → 选择「使用 App 期间」

---

## 9. 快速启动脚本（可选）

在 Git Bash 中依次执行：

```bash
# 终端 1：启动后端
cd /d/课程/大三下/小学期/smart-parking/backend
mvn spring-boot:run

# 终端 2：启动管理端
cd /d/课程/大三下/小学期/smart-parking/admin-frontend
npm run dev
```

> MySQL 和 Redis 需提前手动启动。
